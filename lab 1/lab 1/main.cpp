#include "Check_Graph.h"

int main()
{
	//Graph<int> temp;
	//Graph<double> temp;
	Graph<Triangle> temp;
	//Graph<Tetragon> temp;
	//Graph<Pentagon> temp;
	//Graph<string> temp;
	//Graph<vector<vector<int> > > temp;
	//Graph<vector<vector<vector<Triangle> > > > temp;

	check(temp);
}